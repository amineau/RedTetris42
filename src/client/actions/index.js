import * as types from '../constants/ActionTypes'

export const fall = () => {
    return {
        type: types.FALL,
    }
}

export const dive = () => {
    return {
        type: types.DIVE,
    }
}
